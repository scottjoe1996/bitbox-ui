import { BitButton } from '@bitbox-ui/react';
import React from 'react';

const App: React.FC = () => {
  return (
    <div>
      <BitButton variant="outlined" size="large">
        Button
      </BitButton>
    </div>
  );
};

export default App;
